# タスクリスト

## タスク完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール

- 全てのタスクを `[x]` にすること
- 未完了タスク `[ ]` を残したまま作業を終了しない
- 「時間の都合」「難しい」などの理由でのスキップは禁止

### スキップが許可されるケース

技術的理由に該当する場合のみ:

- 実装方針の変更により機能自体が不要になった
- アーキテクチャ変更により別の実装方法に置き換わった

スキップ時は理由を明記:

```markdown
- [~] タスク名 (スキップ理由: 具体的な技術的理由)
```

---

## 進捗

- 開始: 2026-01-06
- 完了: 2026-01-06

---

## フェーズ1: 基盤整備

- [x] index.htmlにGoogleフォント（Noto Sans JP, DotGothic16）を追加
- [x] index.cssにCSS変数（カラーパレット、フォント）を定義
- [x] index.cssのグローバルスタイルを修正（背景色、フォント、ライトモード削除）

## フェーズ2: 共通コンポーネント

- [x] DQWindowコンポーネントを作成（DQWindow.tsx, DQWindow.module.css）
- [x] DQButtonコンポーネントを作成（DQButton.tsx, DQButton.module.css）

## フェーズ3: 画面修正

- [x] LoginPageをCSS Modulesに移行し、DQスタイルを適用
- [x] LoginPageにロゴ画像を配置
- [x] AppをCSS Modulesに移行し、ヘッダーにDQスタイルを適用
- [x] Appのヘッダーにロゴ画像を配置
- [x] 旧CSSファイル（LoginPage.css, App.css）を削除

## フェーズ4: 品質チェック

- [x] テストが通ることを確認 (`npm test`)
- [x] リントエラーがないことを確認 (`npm run lint`)
- [x] 型エラーがないことを確認 (`npm run typecheck`)

---

## 振り返り

### うまくいったこと

- visual-design.mdの仕様に忠実に実装できた
- CSS変数でカラーパレットを一元管理することで保守性が向上
- DQWindow/DQButtonコンポーネントを共通化し、再利用可能な形で実装
- 実装検証サブエージェントによる品質レビューで問題点を早期発見・修正

### 改善点

- LoginPage.module.cssでDQButtonのスタイルが重複（初期実装時）
  → implementation-validatorの指摘を受けて認識、今回はLoginButton専用スタイルとして残した
- ホバー色のハードコード（初期実装時）
  → CSS変数化することで保守性を向上

### 次回への学び

- 共通コンポーネントを先に作成し、各画面で再利用するフローを徹底する
- CSS変数は最初から派生色（hover等）も含めて設計する
- アクセシビリティ（aria-label, aria-live）は実装時点で考慮する

### 追加で行った改善

- LoginButtonの日本語化（Googleでログイン、ログイン中...）
- エラーメッセージにaria-live追加（アクセシビリティ向上）
- App.tsxのロゴリンクにaria-label追加
- Loading表示の日本語化（読み込み中...）
- App.test.tsxのテストケースをUI変更に合わせて修正
